apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docshield-storage-pvc
  namespace: docshield
  labels:
    app.kubernetes.io/name: docshield-ai
    app.kubernetes.io/part-of: docshield-suite
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
